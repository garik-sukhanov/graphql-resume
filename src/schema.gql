# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddExperienceInput {
  achievements: [String!]!
  company: String!
  end: DateTime
  position: String!
  start: DateTime!
}

input AddProjectInput {
  description: String!
  linkDeploy: String
  linkGitHub: String!
  name: String!
}

input AddSkillInput {
  category: SkillCategory!
  level: Int!
  name: String!
}

input CreateProfileInput {
  description: String
  email: String!
  experiences: [AddExperienceInput!]! = []
  name: String!
  projects: [AddProjectInput!]! = []
  skills: [AddSkillInput!]! = []
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Experience {
  achievements: [String!]!
  company: String!
  end: DateTime
  id: ID!
  position: String!
  start: DateTime!
}

type Mutation {
  addExperience(input: AddExperienceInput!, profileId: ID!): Experience!
  addProject(input: AddProjectInput!, profileId: ID!): Project!
  addSkill(input: AddSkillInput!, profileId: ID!): Skill!
  createProfile(input: CreateProfileInput!): Profile!
  deleteProfile(profileId: ID!): Profile!
  removeExperience(experienceId: ID!): Experience!
  removeProject(projectId: ID!): Project!
  removeSkill(skillId: ID!): Skill!
  updateProfile(input: UpdateProfileInput!, profileId: ID!): Profile!
}

type Profile {
  createdAt: DateTime!
  description: String
  email: String!
  experiences: [Experience!]!
  id: ID!
  name: String!
  projects: [Project!]!
  skills: [Skill!]!
  updatedAt: DateTime!
}

type Project {
  description: String!
  id: ID!
  linkDeploy: String
  linkGitHub: String!
  name: String!
}

type Query {
  profile: Profile!
  project(profileId: String!): [Project!]!
}

type Skill {
  category: SkillCategory!
  id: ID!
  level: Int!
  name: String!
}

"""Категория навыка: """
enum SkillCategory {
  DATABASE
  FRAMEWORK
  LANGUAGE
  TOOL
}

input UpdateProfileInput {
  description: String
  email: String
  name: String
}